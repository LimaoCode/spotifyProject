import React from 'react';

function SingupLogin() {
    return (
        <section className="login-section">
            <div className="login-signup-container">
                <div className="login-signup-wrap">
                    <h3 className="login-subtitulo">Não tem uma conta?</h3>
                    <button type="button" className="login-botao-cadastrar">inscrever-se no spotify</button>
                </div>
            </div>
        </section>
    );
}

export default SingupLogin;